version: '3'
services:
  redis:
    image: redis
    ports:
      - 6379:6379

  fastapi:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    depends_on:
      - redis
    environment:
      - FASTAPI_HOST=0.0.0.0
      - REDIS_HOST=redis